# adaptive_widgets_flutter example

This example app showcases the adaptive dialog, bottom action sheet, date picker,
time picker, and refreshable scroll view included in `adaptive_widgets_flutter`.

Run the app with:

```bash
flutter run
```
