name: adaptive_widgets_flutter
description: "A Flutter package for supporting adaptive widgets for iOS and Android platform."
version: 0.1.1
homepage: https://github.com/karseng9898/flutter_adaptive_widgets.git

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  collection: ^1.18.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

